[package]
name = "noise-io"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
csv = "1.3"
hound = "3.5"
noise-types = { path = "../noise-types" }
symphonia = { version = "0.5", features = ["aac", "alac", "flac", "isomp4", "mp3", "ogg", "pcm", "vorbis", "wav"] }

[dev-dependencies]
tempfile = "3"

[lib]
path = "src/lib.rs"
